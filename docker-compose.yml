services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - UPLOAD_DIR=/data/uploads
      - DATABASE_URL=sqlite+aiosqlite:////data/nivpro.db
    volumes:
      - nivpro_data:/data
    restart: unless-stopped

volumes:
  nivpro_data:
